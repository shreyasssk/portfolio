name: CI
on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Installing necessary pkgs
        run: yarn --prod
      
      - name: Deploying app
        run: yarn run deploy
